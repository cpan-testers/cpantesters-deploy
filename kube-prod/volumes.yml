---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: metabase-tail-log
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn
  resources:
    requests:
      storage: 10Mi
